{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Rust CLI",
            "program": "${workspaceFolder}/target/debug/pyexpr-parser-cli",
            "args": ["[1,2,3]"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "rust: cargo build"
        }
    ]
}